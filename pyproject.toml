[project]
name = "ihavenoideawhatidoanymore"
version = "0.1.0"
description = "A directory of plural people whose job titles stopped working."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "jinja2>=3.1",
    "sqlalchemy>=2.0",
    "python-multipart>=0.0.12",
    "psycopg[binary]>=3.2",
]

[tool.uv]
package = false
